
var btn = document.getElementById("myBtn");

// Подключение обработки событий для формы обратного звонка
var callbackForm = document.getElementById('callbackForm');
callbackForm.addEventListener('submit', validateForm);
